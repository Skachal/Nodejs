<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>

  <body>
    <h1>contacts:</h1>
    <% if (contacts.length) { %> <% contacts.forEach(contact => { %>
    <li>
      <a href="<%= contact.link %>"><%= contact.name %></a>
    </li>
    <% }); %> <% } %> <%- include('./partials/nav.ejs') %>
  </body>
</html>
